{% extends 'base.html.twig' %}

{% block title %}Commande{% endblock %}

{% block body %}

        {{ form_errors(formAdresse) }}

        {{ form_start(formAdresse) }}
        
            <div class='container mt-4'>
                <div class='row justify-content-center'>
                    <h1>Adresse de livraison : </h1>

                    <div class="col-12 mb-3">
                        {{ form_row(formAdresse.nom) }}
                    </div>
                
                    <div class="col-12 mb-3">
                        {{ form_row(formAdresse.prenom) }}
                    </div>
                
                    <div class="col-12 mb-3">
                        {{ form_row(formAdresse.telephone) }}
                    </div>
                
                    <div class="col-12 mb-3">
                        {{ form_row(formAdresse.adresse) }}
                    </div>
                    
                    <div class="col-6 mb-3">
                        {{ form_row(formAdresse.cp) }}
                    </div>
                    
                    <div class="col-6 mb-3">
                        {{ form_row(formAdresse.ville) }}
                    </div>


                        {# {{ form_start(formPaiement) }}
        <div class='container mt-4'>
            <div class='row justify-content-center position-relative'>

            <h1>Moyen de paiement : </h1>

                <div class="col-6 mb-3">
                    {{ form_row(formPaiement.numeros_de_carte) }}
                </div>

                <div class="col-6 mb-3">
                    {{ form_row(formPaiement.expiration) }}
                </div>

                <div class="col-12 mb-3">
                    {{ form_row(formPaiement.code_securite) }}
                </div>

                <div class="col-12 mb-3">
                    {{ form_row(formPaiement.nom_titulaire) }}
                </div>
                
            </div>
        </div>
        
    {{ form_end(formPaiement) }} #}
                
                    <div class="col-6 mb-3">
                        <div>
                            <a href="{{ path ('app_pdf') }}" title="po_confidentialité">
                                {{ form_label(formAdresse.agreeTerms) }}
                            </a>
                            {{ form_widget(formAdresse.agreeTerms) }}
                            {{ form_help(formAdresse.agreeTerms) }}
                            <div class="formAdresse-error">
                                {{ form_errors(formAdresse.agreeTerms) }}
                            </div>
                        </div>
                    </div>

                    <div class="col-6 mb-3">
                        {{ form_row(formAdresse.save) }}
                    </div>

                </div>
            </div>

        {{ form_end(formAdresse) }}

                {{ form_errors(formAdresse) }}

{% endblock %}
